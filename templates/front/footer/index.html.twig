<footer class="footer mt5">
    <div class="container footer-container">
        <div>
            <h6 class="h4 my1 text-upper">{{ 'ui.about'|trans }}</h6>
            <p class="text-justify">{{ 'footer'|trans }}</p>
        </div>

        <div>
            <h6 class="h4 my1 text-upper">Categories</h6>
            <ul>
                <li><a href="{{ path('table.index') }}">{{ 'ui.tables'|trans }}</a></li>
                <li><a href="{{ path('event.index') }}">{{ 'ui.events'|trans }}</a></li>
            </ul>
        </div>

        <div>
            <h6 class="h4 my1 text-upper">{{ 'ui.quick_links'|trans }}</h6>
            <ul>
                <li><a href="{{ path('contact.index') }}">{{ 'ui.contact'|trans }}</a></li>
                <li><a href="{{ path('security.terms-and-conditions') }}">{{ 'ui.agree_terms'|trans }}</a></li>
                <li><a href="{{ path('security.privacy-policy') }}">{{ 'ui.privacy_policy'|trans }}</a></li>
                <li><a href="{{ path('security.cookie-policy') }}">{{ 'ui.cookies_policy'|trans }}</a></li>
            </ul>
        </div>
    </div>

    <div class="container">
        <hr class="my2">
        <div class="flex">
            <p class="copyright-text">
                Copyright &copy; 2021 - {{ "now"|date("Y") }} {{ 'ui.all_rights_reserved'|trans|capitalize }}.
            </p>
        </div>
    </div>
    <form class="form-badge" name="badgeKonami" method="post"
          action="{{ path('badge.unlock') }}?badge=konami">

        <input type="hidden" name="token" value="{{ csrf_token('badge-konami') }}">
    </form>
</footer>
