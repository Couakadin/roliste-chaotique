{% extends '@front/front.html.twig' %}

{% block title %}Newsfeed{% endblock %}

{% block body %}
    <h1>Newsfeed</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ newsfeed.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ newsfeed.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('newsfeed.index') }}">back to list</a>

    <a href="{{ path('newsfeed.edit', {'id': newsfeed.id}) }}">edit</a>

    {{ include('@front/newsfeed/_delete_form.html.twig') }}
{% endblock %}
