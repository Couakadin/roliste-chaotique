{% extends '@email/email.html.twig' %}

{% block subject %}
    {{ subject|capitalize }}
{% endblock %}

{% block email %}
    {{ ('email.new_event_for_user_in_favorite.body'|trans({
        '%signedUrl%': signedUrl,
        '%event%' : event,
        '%user%' : user,
        '%table%': table
        })|UCFirst)|sanitize_html
    }}
{% endblock %}
